# Assets

This folder contains static files that ship *with* the app (not user-generated).

- `animals/` — premade or template animals we can start from when generating an animal from sound.
- `audio_presets/` — built-in audio loops/single sounds the user can add without recording.
- `ui_elements/` - png images used for the background.